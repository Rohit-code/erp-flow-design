Continue designing Regnus ERP — this pass covers the App Home/launcher and the IAM (Identity & Access) administration surfaces: Users, Invites, Groups, Policies, Permissions, Audit Log, and the Organization hierarchy. Same dark theme system as before: near-black surface, light-gray/white text, subtly bordered rounded-md cards on a slightly lighter dark panel, uppercase tracked-wide gray micro-labels, one indigo/blue accent used sparingly, monospace for codes, small pill badges for status/type.

Layout shell (used on every screen below): Left sidebar — small crown/logo mark + "Regnus / ERP" wordmark top-left; below it a tenant-switcher pill (company-initials avatar + "Maxicon Container Line" + a small "+" add button); then a contextual nav list that changes per section (e.g. on Home: Home, then a WORKSPACE group with Subscription/Statement of Account/Agreements/Dashboard/AI Assistant; on the Org pages: an ORGANIZATION group with Org Hierarchy/Corporate Groups/Companies/Regions-Head Offices/Branches-OUs/Departments/Org Readiness/Letterhead/Document Numbering/Booking Flow); bottom-left a workspace footer card (avatar + tenant name + "Shipping ERP" subtitle). Top bar — tenant pill, a centered search input ("Search pages, bookings, customers…" with a ⌘K hint), then icon buttons: help, list-view toggle, theme toggle, notification bell (with an unread dot), and the signed-in user's avatar + email + chevron.

Screen 1 — Home / App Launcher. Top row: 4 stat cards side by side, each an uppercase label top-left ("BOOKINGS", "INQUIRIES", "CUSTOMERS", "KYC TASKS"), a small icon top-right in a muted rounded-square, and a large number below (color varies: neutral, neutral, green, amber). Below that, a stack of sections, each starting with a small uppercase gray eyebrow label, followed by a 4-column responsive grid of service cards. Each card: a muted rounded-square icon top-left, a bold title, and one or two lines of gray description underneath (or the placeholder text "Open service" for a stub). Build these sections in this exact order and content:
- IAM — Dashboard, Users, Service Seats, Groups, Policies, Permissions, Audit Log
- WORKSPACE — Subscription, Statement of Account, Agreements, Dashboard ("Role-based operating dashboard"), AI Assistant ("AI copilot for inquiries & recommendations")
- BOOKING PIPELINE — Inquiries ("Rate requests, AI parsing, sales & trade review"), Quotations ("Quotes, negotiation, MRG approval & acceptance"), KYC ("Customer compliance verification & onboarding"), Pre-Bookings ("Provisional holds awaiting confirmation"), Bookings ("Confirmed shipment bookings & job files"), Customers ("Shippers, consignees, agents & credit")
- OPERATIONS & DOCUMENTS — CRO, CDO ("Container delivery orders & depot release"), Shipping Instructions, Bills of Lading ("Issue, release, surrender"), Master BL ("Master bills of lading & consolidation"), Container Tracking ("Container milestones, gate & depot events")
- SAILING & EQUIPMENT — Sailing Schedules ("Voyages, vessels, slot allocation & cutoffs"), Container Fleet ("Owned, leased & SOC")
- RATES & TARIFFS — Tariff Codes, Exchange Rates, Detention, Ground Rent, Dead Freight, Margin Floor Rules
- BILLING & FINANCE — Invoices, Supplier Invoices, Intercompany, Additional Charges, Waivers ("Detention waiver approvals"), Claims ("Cargo damage disputes & settlement"), AR/AP Operations, E-Invoice/IRN, Fixed Assets, Location Onboarding
- MASTER DATA — Ports, Vessels, Depots, Terminals, Commodities, Slot Operators, Codes, Tax Configuration, Account Ledgers, Tax Posting Map, Suppliers
- ORGANIZATION — Org Hierarchy, Corporate Groups, Companies, Regions/Head Offices, Branches/OUs, Departments, Org Readiness, Letterhead, Document Numbering, Booking Flow
- TOOLS — Uploads ("Bulk import jobs — rate sheets & MRG"), Mailbox Integration ("Gmail / Outlook inquiry capture"), Activity Timeline (with a small favorite/star icon)

Screen 2 — Users. A table: avatar-initial, email, a tier badge, a "scope" label (one of "Workspace-wide access level" / "Works across a regional office" / "Assigned to an operational unit"), an active/inactive toggle, last-login, and a "⋮" row-actions button. Top-right: an "Invite" primary button. Row menu on click shows: Edit scope…, Resend invite email, Deactivate user…

Screen 3 — Invite / Edit Scope dialog (same dialog shape reused for both). A modal: email input, a "Role group" dropdown (group names), then a "Belongs to" section with a helper line ("Select the user level, then assign their region and branch") and three cascading dropdowns: Leveegion level, Branch level), Region (populated once Region or Branch is picked),
Operational Unit (populated once Region is picked, disabled until then). Cancel / Sav

Screen 4 — Groups. A table: group name, a "System" badge for built-in groups (e.g. Adtal), member count, attached-policy count. "New Group" button opens a choice: createempty, or create from a managed policy (a searchable policy dropdown + name/description fields). Row click opens a detail panel: member list with add/remove, and attached policies as removable chipswith an "Attach policy" action.
                                                                                                                                                                                                      Screen 5 — Policies. A catalog list: policy name, a scope badge (Tenant-wide / Regionnt. Row click opens detail: the policy's resource:action permission list grouped bycategory (Booking, Quotation, KYC, Tariff, Finance, etc.) as checkbox rows. A "New cu.Screen 6 — Permissions. A flatter, denser table view: resource, action, effect (Allowranted to — the raw grant list beneath the policy abstraction.Screen 7 — Audit Log. A reverse-chronological table: timestamp, actor, action (verb bort detail string. Filterable by actor and date range.Screen 8 — Org Hierarchy. Breadcrumb trail at top: "Corporate Group · Company · Regio· Department". Below it, a summary header card: org-tree icon, "Maxicon Container LineCorporate Group" title with "Corporate Group · CORP" subtitle, and 4 inline mini statnies, Regions/HOs, Branches/OUs, Departments — each a big number over a small label).Below that, a nested expandable tree: a Company row (building icon, name, code, country) containing an indented Head Office row (bank/institution icon, name, code, country · base currency · an "e-invoice" pill toggle, branch count, chevron), which expands into further-indented Branch/OU rows (building icon, name, code, city, currency, a "N dept" pill) — some branch rows show small tag badges beneath the name for assigned departments (Depot, Kyc, Sales, Trade). Rows arer/selected states, generous vertical rhythm, no connecting lines — indentation aloneconveys hierarchy.